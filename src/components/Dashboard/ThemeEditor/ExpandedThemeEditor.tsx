/**
 * Expanded Theme Editor with Tabs
 * 
 * This is the main expanded theme editor component that includes all tabs:
 * - Visual (colors, typography, spacing, animations)
 * - Layout (structure, components)
 * - Branding (logo, contact, legal)
 * - Features (toggle switches for all features)
 * - Texts (TipTap editors for custom texts)
 * - Components (component-specific settings)
 * - Preview (live preview)
 */

export { ThemeEditor as ExpandedThemeEditor } from './ThemeEditor';

